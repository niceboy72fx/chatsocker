// import { useReducer } from "react";

// const initState = {};

// const StoreReducer: React.FC<{ state: any; action: any }> = ({
//   state,
//   action,
// }) => {
//   switch (action.type) {
//     case "CHANGESIDE":
//       return { ...state, count: state.count + 1 };
//     case "":
//       return { ...state, count: state.count - 1 };
//     default:
//       return state;
//   }
// };

// export const ChangeMessage = { initState, StoreReducer };

// //----------------------------------------------------------------
